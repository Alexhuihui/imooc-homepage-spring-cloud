#spring:
#  application:
#    name: homepage-eureka
#server:
#  port: 8000
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    # 表示是否从Eureka Server中获取注册信息，默认是true。单节点不需要从其他Server中获取信息。
#    fetch-registry: false
#    # 表示是否将自己注册到Eureka Server中，默认是true。单节点不需要。
#    register-with-eureka: false
#    service-url:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
